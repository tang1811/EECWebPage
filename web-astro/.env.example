# GA4 web-stream Measurement ID. Leave empty to disable analytics collection.
# Enable COOKIE_CONSENT_ENABLED in src/config.ts to show the consent banner.
# With consent enabled but no ID, the banner works without collecting analytics.
# Never put a secret API key here.
PUBLIC_GA_ID=
